---
import Layout from '../layouts/Layout.astro';
import { treks } from '../data/treksData.js';
import { generalFAQs } from "../data/faqsData.js";
import NewsBanner from "../components/NewsBanner.jsx";
import NewsSection from "../components/NewsSection.jsx";
import "../styles/global.css";
---

<Layout title="Planethimalayas">

<!-- Top banner -->
<div class="banner-wrapper">
  <NewsBanner client:load />
</div>

<!-- Hero Carousel Section -->
<section class="hero-carousel reveal">
  <div class="carousel-track">
    <div class="carousel-slide active" style="background-image: url('https://res.cloudinary.com/dr44hob9b/image/upload/v1761810023/love-himalayas_zz3cyf.jpg');">
      <div class="overlay"></div>
      <div class="hero-content">
        <h1>More Than Just A Trek</h1>
        <a href="/upcoming-treks" class="cta-button">Upcoming Treks</a>
        <p class="hero-subheading">
          Join our treks, thoughtfully crafted for deeply personal mountain experiences with unwavering focus on individual care, safety, and a genuine love for the Himalayas.
        </p>
      </div>
    </div>

    <div class="carousel-slide" style="background-image: url('https://res.cloudinary.com/dr44hob9b/image/upload/v1761810048/journey-1_w9t3zk.jpg');">
      <div class="overlay"></div>
      <div class="hero-content">
        <h1>Not Just a Trek — A Journey That Stays With You</h1>
        <a href="/upcoming-treks" class="cta-button">Upcoming Treks</a>
        <p class="hero-subheading">
          Every trail we walk is a carefully chosen path into the heart of the Himalayas — not just to witness their grandeur, but to feel them under your skin and soul.
        </p>
      </div>
    </div>

    <div class="carousel-slide" style="background-image: url('https://res.cloudinary.com/dr44hob9b/image/upload/v1761809893/care_pzucvr.jpg');">
      <div class="overlay"></div>
      <div class="hero-content">
        <h1>Individual Care, Every Step of the Way</h1>
        <a href="/upcoming-treks" class="cta-button">Upcoming Treks</a>
        <p class="hero-subheading">
          We keep our groups small so we can focus on you — your pace, your concerns, your joy. Our leaders walk with intention, offering safety and attention without intrusion.
        </p>
      </div>
    </div>

    <div class="carousel-slide" style="background-image: url('https://res.cloudinary.com/dr44hob9b/image/upload/v1761810048/love-himalayas-3_fpus1m.jpg');">
      <div class="overlay"></div>
      <div class="hero-content">
        <h1>Built on Genuine Love for the Himalayas</h1>
        <a href="/upcoming-treks" class="cta-button">Upcoming Treks</a>
        <p class="hero-subheading">
          We live and breathe these mountains. With Planethimalayas, your trek becomes a way of giving back to the Himalayas that give us so much.
        </p>
      </div>
    </div>
  </div>

  <!-- Arrows (for desktop) -->
  <button class="carousel-arrow left">&#10094;</button>
  <button class="carousel-arrow right">&#10095;</button>

  <!-- Dots (for mobile + desktop visual feedback) -->
  <div class="carousel-dots">
    <button class="active"></button>
    <button></button>
    <button></button>
    <button></button>
  </div>
</section>

<div class="container">
<!-- Testimonials Section -->
<section class="testimonials reveal">
  <h2 class="testimonials-heading">What our <span>trekkers say</span></h2>
  <div class="section-underline"></div>

  <!-- Wrapper to hold arrows + cards -->
  <div class="testimonials-wrapper" style="position: relative;">
    <!-- Left Arrow -->
    <button class="scroll-btn left" onclick="scrollTestimonials(-1)">&#10094;</button>
    <!-- Right Arrow -->
    <button class="scroll-btn right" onclick="scrollTestimonials(1)">&#10095;</button>

    <!-- Card Container -->
    <div class="testimonials-card-wrapper" id="testimonialRow">
      
      <!-- Testimonial Card 1 -->
      <div class="testimonial-card">
        <div class="testimonial-header">
          <img src="https://res.cloudinary.com/dr44hob9b/image/upload/v1762540205/Screenshot_20251107-235350.Instagram_nvs4zy.jpg" alt="Ananya Mitra" class="testimonial-pic" />
          <div class="testimonial-info">
            <h3 class="testimonial-name">Ananya Mitra</h3>
            <p class="testimonial-profession">Kolkata</p>
            <div class="testimonial-social">
              <a href="https://instagram.com/ananya_mitra22" target="_blank">
                <i class="fab fa-instagram"></i> @ananya_mitra22
              </a>
            </div>
            <div class="testimonial-stars">★★★★★</div>
          </div>
        </div>
        <p class="testimonial-text">
          Kamal Das the leader and founder is a great person and had good knowledge about the route. The food was great and the stays were comfortable which was above the expectation in that altitude.<br>Thanks to Planethimalayas.
        </p>
        <button class="read-more-btn">Read More</button>
      </div>

      <!-- Testimonial Card 2 -->
      <div class="testimonial-card">
        <div class="testimonial-header">
          <img src="/images/main/jytte.png" alt="Jytte Scheepens" class="testimonial-pic" />
          <div class="testimonial-info">
            <h3 class="testimonial-name">Jytte Scheepens</h3>
            <p class="testimonial-profession">Freelancer | Breda, Netherlands</p>
            <div class="testimonial-social">
              <a href="https://instagram.com/flowofjytte" target="_blank">
                <i class="fab fa-instagram"></i> @flowofjytte
              </a>
            </div>
            <div class="testimonial-stars">★★★★★</div>
          </div>
        </div>
        <p class="testimonial-text">
        I did the Kashmir Great Lakes trek with kamal. This was such an unforgettable experience. Kamal is such an amazing guide. He told us so many interesting facts, I had beautiful deep talks with him about the mountains and listened to many of his stories about the experiences he's had while trekking. he's such a genuine and pure soul. I was the only female in the team, but not once I felt uncomfortable or left out. Everyone was increrdibly respectful and nice. We were allowed to walk on our own pace and didn't feel rushed or pushed. Everone can do the trek as they want, in the way they want. He respects you and really goes out of his way to make you enjoy the most. Till this day he's still a dear friend of mine and I hope to have the priviledge to go to the mountains again, together with him. If you're planning to do a trek, definitely do it with Kamal, he a 100% will make sure this trip is something you'll never forget about.
        </p>
        <button class="read-more-btn">Read More</button>
      </div>

      <!-- Testimonial Card 3 -->
      <div class="testimonial-card">
        <div class="testimonial-header">
          <img src="/images/main/clare.jpg" alt="Clare Fisher" class="testimonial-pic" />
          <div class="testimonial-info">
            <h3 class="testimonial-name">Clare Fisher</h3>
            <p class="testimonial-profession">Marketing | Canada</p>
            <div class="testimonial-social">
              <a href="https://instagram.com/clare.fisher" target="_blank">
                <i class="fab fa-instagram"></i> @clare.fisher
              </a>
            </div>
            <div class="testimonial-stars">★★★★★</div>
          </div>
        </div>
        <p class="testimonial-text">
          I had some nerves about going on a trek solo but Kamal took excellent care of me and made sure I felt safe and looked after. He was able to customize the trek to my needs and we went on lots of side quests to explore the area. Kamal is very knowledgable and has some fantastic stories to tell, so was the best company the entire time – it felt like I had friends adventuring with me! Thanks again :)
        </p>
        <button class="read-more-btn">Read More</button>
      </div>

      <!-- Testimonial Card 4 -->
      <div class="testimonial-card">
        <div class="testimonial-header">
          <img src="https://res.cloudinary.com/dr44hob9b/image/upload/v1762540205/Screenshot_20251107-235337.Instagram_itofyg.jpg" alt="Suramita Chakrabarty" class="testimonial-pic" />
          <div class="testimonial-info">
            <h3 class="testimonial-name">Suramita Chakrabarty</h3>
            <p class="testimonial-profession">Kolkata</p>
            <div class="testimonial-social">
              <a href="https://instagram.com/suramitachakrabarty" target="_blank">
                <i class="fab fa-instagram"></i> @suramitachakrabarty
              </a>
            </div>
            <div class="testimonial-stars">★★★★★</div>
          </div>
        </div>
        <p class="testimonial-text">
          Amazing staff with good and decent behaviour. Food quality was also very good. Through out the trek they had taken utmost care of everything.<br>Will definitely plan again with Mr. Kamal Das.
        </p>
        <button class="read-more-btn">Read More</button>
      </div>

      <!-- Testimonial Card 5 -->
      <div class="testimonial-card">
        <div class="testimonial-header">
          <img src="/images/main/samar.jpeg" alt="Samar" class="testimonial-pic" />
          <div class="testimonial-info">
            <h3 class="testimonial-name">Samar</h3>
            <p class="testimonial-profession">Automotive UX Designer | Pune</p>
            <div class="testimonial-social">
              <a href="https://instagram.com/wheelspin_199" target="_blank">
                <i class="fab fa-instagram"></i> @wheelspin_199
              </a>
            </div>
            <div class="testimonial-stars">★★★★★</div>
          </div>
        </div>
        <p class="testimonial-text">
          I made friendship with the mountains through Kamal sirs experience. The moment i met him at my last trek at Kedarkantha, I was impressed by his mountaineering knowledge and friendly nature that this year too (to Tungnath) and every year hereafter I am going to partner him to a new peak and new experiences in those journeys. Kudos to Planethimalayas for the exceptional work in making the love of mountains more accessible and enjoyable.
        </p>
        <button class="read-more-btn">Read More</button>
      </div>

      <!-- Testimonial Card 6 -->
      <div class="testimonial-card">
        <div class="testimonial-header">
          <img src="/images/main/akshata.jpg" alt="Akshata Poojari" class="testimonial-pic" />
          <div class="testimonial-info">
            <h3 class="testimonial-name">Akshata Poojari</h3>
            <p class="testimonial-profession">Bangalore</p>
            <div class="testimonial-social">
              <a href="https://instagram.com/akshata_r_poojari" target="_blank">
                <i class="fab fa-instagram"></i> @akshata_r_poojari
              </a>
            </div>
            <div class="testimonial-stars">★★★★★</div>
          </div>
        </div>
        <p class="testimonial-text">
          The trekking experience was absolutely unforgettable..Our guide was friendly and knowledgeable, making the whole journey fun and safe.
        </p>
        <button class="read-more-btn">Read More</button>
      </div>


    </div> <!-- /#testimonialRow -->
  </div> <!-- /.testimonials-wrapper -->
</section> <!-- /.testimonials -->

<!-- UPCOMING TREKS: Winter Treks -->
<section class="trek-section reveal">
  <h2>Upcoming <span>Treks</span></h2>
  <div class="section-underline"></div>
  <div class="trek-cards-wrapper">
    <button class="scroll-btn left" onclick="scrollCards('winter', -1)">&lt;</button>
    <div class="trek-cards-scroll" id="winter">
      {
        treks
          .filter(trek => Array.isArray(trek.season) && trek.season.map(s => s.toLowerCase()).includes('winter'))
          .map(trek => {
            const duration = trek.overview?.facts?.find(f => f.label === "Trek Duration")?.value || trek.duration;
            const difficultyStr = Array.isArray(trek.difficulty) ? trek.difficulty.join(', ') : trek.difficulty;
            return (
              <div class="trek-card" key={trek.id}>
                <a href={`/trek/${trek.id}#trek-hero`}>
                  <img src={trek.heroImage} alt={`View of ${trek.name} trek`} loading="lazy" />
                </a>
                <h3 class="trek-name">{trek.name}</h3>
                <div class="trek-info">{duration} | {trek.region}</div>
                <div class="trek-difficulty">{difficultyStr}</div>
                <div class="trek-buttons">
                  <a href={`/trek/${trek.id}#highlights`} class="btn btn-details">View Details</a>
                  <a href={`/trek/${trek.id}#pricing-snippet-wrapper`} class="btn btn-dates">View Dates</a>
                </div>
              </div>
            );
          })
      }
    </div>
    <button class="scroll-btn right" onclick="scrollCards('winter', 1)">&gt;</button>
  </div>
</section>

<!-- News Section -->
<section class="news-section reveal">
  <h2 class="news-section-heading">News & <span>Articles</span></h2>
  <div class="section-underline"></div>
  <NewsSection client:load />
</section>

<!-- Faqs Section -->
<section class="faqs-section reveal">
    <h2 class="faqs-section-heading">FAQ<span>s</span></h2>
    <div class="section-underline"></div>

    <!-- FAQ topics -->
<!-- FAQ topics -->
{generalFAQs.map((section, sectionIndex) => (
          <div class="faq-topic" id={`faq-topic-${sectionIndex}`}>
            <button class="faq-topic-header" aria-expanded="false">
              {section.topic}
              <span class="toggle">▶</span>
            </button>
            <div class="faq-topic-body" hidden>
              {section.sections
                ? section.sections.map((sub, subIndex) => (
                    <div class="faq-subtopic" id={`faq-${sectionIndex}-${subIndex}`}>
                      <button class="faq-subtopic-header" aria-expanded="false">
                        {sub.subtopic}
                        <span class="toggle">▶</span>
                      </button>
                      <div class="faq-subtopic-body" hidden>
                        {sub.faqs.map((faq, faqIndex) => (
                          <div class="faq-item" id={`faq-${sectionIndex}-${subIndex}-${faqIndex}`}>
                            <button class="faq-question" aria-expanded="false">
                              {faq.question}
                              <span class="toggle">+</span>
                            </button>
                            <div class="faq-answer" hidden>
                              <div set:html={faq.answer}></div>
                            </div>
                          </div>
                        ))}
                      </div>
                    </div>
                  ))
                : section.faqs.map((faq, faqIndex) => (
                    <div class="faq-item" id={`faq-${sectionIndex}-${faqIndex}`}>
                      <button class="faq-question" aria-expanded="false">
                        {faq.question}
                        <span class="toggle">+</span>
                      </button>
                      <div class="faq-answer" hidden>
                        <div set:html={faq.answer}></div>
                      </div>
                    </div>
              ))}
            </div>
          </div>
))}

    <!-- Link to full FAQs page -->
    <div class="faq-footer">
      <a href="/faqs" target='_blank' rel='noopener noreferrer'>Explore all FAQs →</a>
    </div>
</section>


<style>

.banner-wrapper {
  width: 100%;
  height: 30px;
  position: absolute;   /* pulls it out of flow */
  top: 110px;           /* two navbars stacked (55px each) */
  z-index: 90;          /* above hero but below navbars */
  background: transparent;
  overflow: hidden;
}

@media (max-width: 768px) {
  .banner-wrapper {
    top: 55px; /* only one navbar in mobile */
  }
}

.hero-carousel {
  position: relative;
  width: 100%;
  height: 100vh;
  overflow: hidden;
  color: white;
}

/* Smooth fade + slide blend transition */
.carousel-track {
  display: flex;
  position: relative;
  height: 100%;
}

.carousel-slide {
  position: absolute;
  inset: 0;
  opacity: 0;
  transition: opacity 1.5s cubic-bezier(0.4, 0, 0.2, 1),
              transform 1.5s cubic-bezier(0.4, 0, 0.2, 1);
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  transform: scale(1.05);
}

/* Active slide (visible) */
.carousel-slide.active {
  opacity: 1;
  transform: scale(1);
  z-index: 2;
}

/* Keep other slides behind */
.carousel-slide:not(.active) {
  z-index: 1;
}

.overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    rgba(0, 0, 0, 0.25),
    rgba(0, 0, 0, 0.25)
  );
  z-index: 1;
}

.hero-content {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  z-index: 2;
  width: 85%;
  max-width: 1000px;
}

.hero-content h1 {
  font-family: Helvetica, sans-serif;
  font-size: 4rem;
  font-weight: bold;
  margin-bottom: 3rem;
  color: white;
  text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.6);
}

.hero-subheading {
  font-family: "Roboto", sans-serif;
  font-size: 1.5rem;
  margin-top: 1.5rem;
  color: white;
  text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
}

.cta-button {
  display: inline-block;
  background: #ff7300;
  color: black;
  font-family: "Roboto", sans-serif;
  font-weight: 700;
  padding: 0.7rem 2.5rem;
  border-radius: 8px;
  text-decoration: none;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.6);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.cta-button:hover {
  transform: scale(1.05);
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.8);
}

/* Arrows */
.carousel-arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(0, 0, 0, 0.5);
  color: white;
  border: none;
  font-size: 2rem;
  cursor: pointer;
  z-index: 3;
  padding: 0.4rem 0.8rem;
  border-radius: 50%;
  transition: background 0.3s ease;
}

.carousel-arrow:hover {
  background: rgba(0, 0, 0, 0.8);
}

.carousel-arrow.left {
  left: 50px;
}
.carousel-arrow.right {
  right: 50px;
}

/* Dots */
.carousel-dots {
  position: absolute;
  bottom: 25px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 10px;
  z-index: 3;
}

.carousel-dots button {
  appearance: none;
  -webkit-appearance: none;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  border: none;
  background-color: rgba(255, 255, 255, 0.6); /* visible dot */
  cursor: pointer;
  padding: 0;
  margin: 0;
  box-sizing: content-box;
  display: inline-block;
  transition: background 0.3s ease, transform 0.3s ease;
}

.carousel-dots button.active {
  background-color: #ff7300;
  transform: scale(1.2);
}

/* Responsive */
@media (max-width: 768px) {
  .hero-carousel {
    height: 75vh;
    margin-top: -55px;
  }
  .carousel-slide {
    height: 75vh;
  }
  .hero-content h1 {
    font-size: 2rem;
  }
  .hero-subheading {
    font-size: 1rem;
  }
  .cta-button {
    padding: 0.6rem 2rem;
  }
  .carousel-arrow {
    display: none; /* hide arrows on mobile */
  }
}

/* Hide dots on desktop */
@media (min-width: 769px) {
  .carousel-dots {
    display: none;
  }
}

/* Testimonials Section - matching About Section style */
.testimonials {
  background-color: white;
  position: relative;
}

.testimonials-heading {
  font-size: 2rem;
  font-family: 'Helvetica', sans-serif;
  text-align: left;
}

/* Card wrapper */
.testimonials-card-wrapper {
  display: flex;
  gap: 1.5rem;
  overflow-x: auto;
  scroll-behavior: smooth;
  padding-bottom: 10px;
  scroll-snap-type: x mandatory;
}

.::-webkit-scrollbar {
  height: 8px;
}
.::-scrollbar-thumb {
  background-color: #ff7300;
  border-radius: 4px;
}
.::-scrollbar-track {
  background: #f0f0f0;
}

/* Individual cards */
.testimonial-card {
  flex: 0 0 50%; /* 2 cards per screen on desktop */
  max-width: 50%;
  scroll-snap-align: start;
  background: #fff;
  border-radius: 12px;
  box-shadow: 2px 6px 15px rgba(0, 0, 0, 0.3);
  padding: 2rem;
}

.testimonial-header {
  display: flex;
  align-items: center;
  margin-bottom: 1rem;
}

.testimonial-pic {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  object-fit: cover;
  margin-right: 15px;
}

.testimonial-info {
  display: flex;
  flex-direction: column;
}

.testimonial-name {
  font-weight: bold;
  margin: 0;
  font-size: 1rem;
}

.testimonial-profession,
.testimonial-social {
  font-size: 0.85rem;
  color: #666;
  margin: 2px 0;
}

.testimonial-stars {
  color: #ff7300;
  font-size: 1rem;
}

.testimonial-text {
  font-size: 1rem;
  line-height: 1.4;
  margin-top: 0.5rem;
  overflow: hidden;
  max-height: 60px;
  transition: max-height 0.3s ease;
}

.testimonial-card.expanded .testimonial-text {
  max-height: 500px;
}

.read-more-btn {
  background: none;
  border: none;
  color: #ff7300;
  cursor: pointer;
  margin-top: 0.5rem;
  align-self: flex-start;
  font-weight: bold;
  font-size: 0.9rem;
  padding: 0;
}

/* Scrollbar styling */
.scroll-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 30px;   /* smaller width */
  height: 30px;  /* smaller height */
  border: none;
  background: rgba(0,0,0,0.3); /* semi-transparent */
  color: #ff7300;
  font-size: 18px; /* arrow size */
  font-weight: bold;
  border-radius: 50%;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;   /* above cards but lightweight */
  box-shadow: 0 2px 6px rgba(0,0,0,0.4);
  transition: background 0.3s;
}

.scroll-btn:hover {
  background: rgba(0,0,0,0.6);
}

.scroll-btn.left {
  left: -15px;   /* slightly outside container so it doesn’t cover content */
}

.scroll-btn.right {
  right: -15px;  /* slightly outside container */
}

/* Optional: disable outline on focus */
.scroll-btn:focus {
  outline: none;
}

/* 3. Social icons */
.testimonial-social a {
  margin-right: 0.5rem;
  color: #E1306C;
  text-decoration: none;
  font-size: 1rem;
  display: inline-flex;
  align-items: center;
  gap: 0.3rem;
  transition: color 0.3s ease;
}

.testimonial-social a:hover {
  color: #ff7300;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .testimonial-card {
    flex: 0 0 100%;   /* 1 per view on mobile */
    max-width: 100%;
  }
}

.trek-section {
  margin-bottom: 2rem;

}
.trek-section h2 {
  font-size: 2rem;
  font-family: 'Helvetica', sans-serif;
  color: black;
  
}

/* Wrapper for scroll + buttons */
.trek-cards-wrapper {
  position: relative;
  width: 100%;
}

/* Horizontal scroll container */
.trek-cards-scroll {
  display: flex;
  gap: 1rem;
  overflow-x: auto;
  scroll-behavior: smooth;
  padding: 1rem 0;
  scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch;
}

/* Individual card */
.trek-card {
  flex: 0 0 33.333%;  /* 3 per row desktop */
  scroll-snap-align: start;
  background: #fff;
  border-radius: 8px;
  box-shadow: 
    0 4px 8px rgba(0, 0, 0, 0.1),        /* subtle base shadow */
    0 8px 20px rgba(0, 0, 0, 0.15);       /* deeper shadow for depth */
  display: flex;
  flex-direction: column;
  overflow: hidden;
  min-width: 0;
  transition: 
    transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), 
    box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    will-change: transform, box-shadow;
}

  .trek-card:hover {
    transform: translateY(-14px) rotateX(4deg) rotateY(2deg);
    box-shadow: 
    0 12px 24px rgba(0, 0, 0, 0.25), 
    0 20px 40px rgba(0, 0, 0, 0.3);
    z-index: 10;
  }

/* Mobile: 2 per row */
@media (max-width: 768px) {
  .trek-card {
    flex: 0 0 100%;
  }
}

/* Card image */
.trek-card img {
  width: 100%;
  height: 160px;
  object-fit: cover;
  transition: transform 0.3s ease;
    will-change: transform;
}

.trek-card:hover img {
    transform: scale(1.25) rotate(-1deg);
}

/* Card text */
.trek-name {
  font-weight: 700;
  font-size: 1.5rem;
  color: #ff7300;
  text-align: center;
  margin: 0.5rem 0;
  text-shadow: 0 1px 2px rgba(0,0,0,0.1);
}

.trek-info, .trek-difficulty {
  text-align: center;
  margin: 0 0.5rem 0.5rem;
  font-size: 1rem;
  color: black;
  font-weight: 700;
  text-transform: capitalize;
}

/* Buttons container */
.trek-buttons {
  display: flex;
  justify-content: center;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
}

/* Buttons */
.trek-card a.btn {
  padding: 0.4rem 0.8rem;
  text-decoration: none;
  font-weight: 600;
  font-size: 0.85rem;
  border-radius: 4px;
  transition: all 0.2s ease;
  border: none;
  cursor: pointer;
  border: 2px solid transparent;
}

.btn-details {
  background: black;
  color: white;
  box-shadow: 0px 2px 8px #ff7300;
}

.btn-details:hover {
  background: #ff7300;
  color: black;
  box-shadow: 0px 2px 8px black;
}

.btn-dates {
  background: #ff7300;
  color: black;
  box-shadow: 0px 2px 8px black;
}

.btn-dates:hover {
  background: black;
  color: white;
  box-shadow: 0px 2px 8px #ff7300;
}

.news-section {
  background-color: white;
  position: relative;
}

.news-section-heading {
  font-size: 2rem;
  font-family: 'Helvetica', sans-serif;
  text-align: left;
}

/* ========================= */
/* 🌄 FAQ SECTION STYLES     */
/* ========================= */

.faqs-section {
  background-color: white;
  position: relative;
  padding: 2rem 0;
}

.faqs-section-heading {
  font-size: 2rem;
  font-family: 'Helvetica', sans-serif;
  text-align: left;
  margin-bottom: 0.5rem;
}

/* --- Topic Level --- */
.faq-topic {
  border: 1px solid #eee;
  border-radius: 6px;
  margin-bottom: 0.8rem;
  overflow: hidden;
}

.faq-topic-header {
  background: #f9f9f9;
  padding: 0.7rem 1rem;
  width: 100%;
  text-align: left;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-family: 'Helvetica', sans-serif;
  font-size: 1.05rem;
  font-weight: bold;
  border: none;
  outline: none;
  transition: background 0.3s ease;
}

.faq-topic-header:hover {
  background: #f3f3f3;
  color: #ff7300;
}

.faq-topic-body {
  padding: 0.5rem 1.2rem 0.5rem 1.2rem;
  background: white;
}

/* --- Subtopic Level --- */
.faq-subtopic {
  margin: 0.8rem 0;
  border-left: 3px solid #ff7300;
  padding-left: 0.8rem;
}

.faq-subtopic-header {
  background: #fff6ef;
  padding: 0.5rem 0.8rem;
  border-radius: 4px;
  width: 100%;
  text-align: left;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-weight: 600;
  font-size: 1rem;
  border: none;
  outline: none;
  transition: color 0.3s ease;
}

.faq-subtopic-header:hover {
  color: #ff7300;
}

.faq-subtopic-body {
  margin-top: 0.3rem;
  margin-left: 1rem;
  padding-left: 0.5rem;
  border-left: 2px dotted #ff7300;
}

/* --- Individual FAQ Items --- */
.faq-item {
  margin-bottom: 0.5rem;
}

.faq-question {
  background: none;
  border: none;
  width: 100%;
  text-align: left;
  cursor: pointer;
  font-weight: bold;
  font-size: 1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.3rem 0;
  transition: color 0.3s ease;
}

.faq-question:hover {
  color: #ff7300;
}

.faq-question[aria-expanded="true"] {
  color: #ff7300;
}

.faq-answer {
  position: relative;
  margin: 0.3rem 0 0.8rem 1.5rem;
  padding-left: 1rem;
  font-size: 1rem;
  line-height: 1.6;
}

.faq-answer::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  width: 4px;
  height: 100%;
  background-color: #ff7300;
  border-radius: 2px;
}

.toggle {
  font-size: 1.2rem;
  transition: transform 0.3s ease;
}

/* --- Footer Link --- */
.faq-footer {
  text-align: left;
  margin-top: 1.5rem;
}

.faq-footer a {
  color: #ff7300;
  font-weight: bold;
  font-size: 1rem;
  text-decoration: none;
}

.faq-footer a:hover {
  text-decoration: underline;
}

.testimonials.reveal h2 span {
  color: #ff7300;
}

.trek-section.reveal h2 span {
  color: #ff7300;
}

.news-section.reveal h2 span {
  color: #ff7300;
}

.faqs-section.reveal h2 span {
  color: #ff7300;
}
</style>

</div>
</Layout>

<script>
document.addEventListener("DOMContentLoaded", function () {
  const row = document.getElementById("testimonialRow");
  const leftBtn = document.querySelector(".scroll-btn.left");
  const rightBtn = document.querySelector(".scroll-btn.right");

  // Scroll Function
  function scrollTestimonials(direction) {
    const card = row.querySelector(".testimonial-card");
    if (!card) return;

    const gap = parseInt(getComputedStyle(row).gap) || 0;
    const cardWidth = card.offsetWidth + gap;

    row.scrollBy({
      left: direction * cardWidth,
      behavior: "smooth"
    });

    setTimeout(updateArrowState, 400); // update after scroll animation
  }

  // Enable/Disable Arrows at edges
  function updateArrowState() {
    leftBtn.disabled = row.scrollLeft <= 0;
    rightBtn.disabled = row.scrollLeft + row.clientWidth >= row.scrollWidth;
  }

  // Attach Events to arrows
  leftBtn.addEventListener("click", () => scrollTestimonials(-1));
  rightBtn.addEventListener("click", () => scrollTestimonials(1));

  // Initial state
  updateArrowState();

  // Read More Toggle
  document.querySelectorAll('.read-more-btn').forEach(btn => {
    btn.addEventListener('click', function() {
      const card = btn.closest('.testimonial-card');
      card.classList.toggle('expanded');
      btn.textContent = card.classList.contains('expanded') ? 'Read Less' : 'Read More';
    });
  });
});
</script>


<script type="module">
function scrollCards(sectionId, direction) {
  const container = document.getElementById(sectionId);
  if (!container) return;

  // Calculate scroll by card width + gap
  const card = container.querySelector('.trek-card');
  if (!card) return;
  
  const style = window.getComputedStyle(container);
  const gap = parseInt(style.gap) || 16; // default 1rem if not set
  const scrollAmount = (card.offsetWidth + gap) * direction;

  container.scrollBy({
    left: scrollAmount,
    behavior: 'smooth'
  });
}

// Expose globally so onclick works
window.scrollCards = scrollCards;
</script>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const faqTopics = document.querySelectorAll(".faq-topic");

    // --- Topic Toggle ---
    faqTopics.forEach(topic => {
      const header = topic.querySelector(".faq-topic-header");
      const body = topic.querySelector(".faq-topic-body");

      header.addEventListener("click", () => {
        const expanded = header.getAttribute("aria-expanded") === "true";
        header.setAttribute("aria-expanded", !expanded);
        body.hidden = expanded;
        body.style.display = expanded ? "none" : "block";
        header.querySelector(".topic-toggle").textContent = expanded ? "▶" : "▼";
      });
    });

    // --- Subtopic Toggle ---
    document.querySelectorAll(".faq-subtopic").forEach(sub => {
      const subHeader = sub.querySelector(".faq-subtopic-header");
      const subBody = sub.querySelector(".faq-subtopic-body");
      if (!subHeader || !subBody) return;

      subHeader.addEventListener("click", () => {
        const expanded = subHeader.getAttribute("aria-expanded") === "true";
        subHeader.setAttribute("aria-expanded", !expanded);
        subBody.hidden = expanded;
        subBody.style.display = expanded ? "none" : "block";
        subHeader.querySelector(".subtopic-toggle").textContent = expanded ? "▸" : "▾";
      });
    });

    // --- FAQ Question Toggle ---
    document.querySelectorAll(".faq-item").forEach(item => {
      const btn = item.querySelector(".faq-question");
      const answer = item.querySelector(".faq-answer");
      if (!btn || !answer) return;

      btn.addEventListener("click", () => {
        const expanded = btn.getAttribute("aria-expanded") === "true";
        btn.setAttribute("aria-expanded", !expanded);
        answer.hidden = expanded;
        btn.querySelector(".faq-toggle").textContent = expanded ? ">" : "v";
      });
    });
  });
</script>

<script>
const heroContainer = document.querySelector(".hero .container");
const hero = document.querySelector(".contact-hero");

window.addEventListener("scroll", () => {
  const heroRect = hero.getBoundingClientRect();
  const scrollY = window.scrollY;

  if (heroRect.bottom > 0) {
    let move = scrollY * 0.5;                
    const maxMove = hero.offsetHeight / 2;

    heroContainer.style.transform = `translateY(${move}px)`;
  }
});
</script>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const slides = document.querySelectorAll(".carousel-slide");
  const leftArrow = document.querySelector(".carousel-arrow.left");
  const rightArrow = document.querySelector(".carousel-arrow.right");
  const dots = document.querySelectorAll(".carousel-dots button");
  const hero = document.querySelector(".hero-carousel");

  let currentIndex = 0;
  let autoSlide;

  function updateUI() {
    slides.forEach((slide, index) => {
      slide.classList.toggle("active", index === currentIndex);
    });
    dots.forEach((dot, index) => {
      dot.classList.toggle("active", index === currentIndex);
    });
  }

  function showNext() {
    currentIndex = (currentIndex + 1) % slides.length;
    updateUI();
  }

  function showPrev() {
    currentIndex = (currentIndex - 1 + slides.length) % slides.length;
    updateUI();
  }

  function goToSlide(index) {
    currentIndex = index;
    updateUI();
    resetAutoSlide();
  }

  function resetAutoSlide() {
    clearInterval(autoSlide);
    autoSlide = setInterval(showNext, 5000);
  }

  // Dots click
  dots.forEach((dot, index) => {
    dot.addEventListener("click", () => goToSlide(index));
  });

  // Arrows
  leftArrow.addEventListener("click", () => { showPrev(); resetAutoSlide(); });
  rightArrow.addEventListener("click", () => { showNext(); resetAutoSlide(); });

  // Pause on hover
  hero.addEventListener("mouseenter", () => clearInterval(autoSlide));
  hero.addEventListener("mouseleave", resetAutoSlide);

  // Swipe support (for mobile)
  let startX = 0;
  hero.addEventListener("touchstart", e => startX = e.touches[0].clientX);
  hero.addEventListener("touchend", e => {
    const endX = e.changedTouches[0].clientX;
    if (startX - endX > 50) showNext();
    if (endX - startX > 50) showPrev();
    resetAutoSlide();
  });

  // Initialize
  updateUI();
  resetAutoSlide();
});
</script>